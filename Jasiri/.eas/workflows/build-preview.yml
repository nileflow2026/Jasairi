name: Build Preview (Android)

# Triggered automatically on every push to main.
# Produces an internal APK distributed to testers via Expo Go / internal link.

on:
  push:
    branches:
      - main

jobs:
  build_android_preview:
    type: build
    params:
      platform: android
      profile: preview
