name: Mobile — EAS Build & Submit

# ⚠️  Builds and store submissions are now handled natively by EAS Workflows.
# Workflow files live in Jasiri/.eas/workflows/
#
#   build-preview.yml      — APK on every push to main
#   build-production.yml   — AAB + Google Play submit on version tags (v*.*.*)
#
# This file is kept for reference only and has no active jobs.
#
# To trigger builds manually: npx eas-cli@latest workflow:run build-production.yml
# Monitor runs: https://expo.dev/accounts/[account]/projects/jasiri/workflows

on:
  workflow_dispatch: # manual trigger only — does nothing

